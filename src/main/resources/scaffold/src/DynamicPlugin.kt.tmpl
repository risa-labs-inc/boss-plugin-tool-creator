package ai.rever.boss.plugin.dynamic.@@PACKAGE@@

import ai.rever.boss.plugin.api.DynamicPlugin
import ai.rever.boss.plugin.api.PluginContext

class @@CLASS_PREFIX@@DynamicPlugin : DynamicPlugin {
    override val pluginId: String = "@@PLUGIN_ID@@"
    override val displayName: String = "@@TOOL_NAME_KT@@"
    override val version: String = "0.1.0"
    override val description: String = "@@TOOL_DESCRIPTION_KT@@"
    override val author: String = "Risa Labs"
    override val url: String = "https://github.com/risa-labs-inc/@@REPO_NAME@@"

    override fun register(context: PluginContext) {
        context.panelRegistry.registerPanel(@@CLASS_PREFIX@@Info) { ctx, panelInfo ->
            @@CLASS_PREFIX@@Component(ctx, panelInfo, context)
        }
    }

    override fun dispose() {}
}
